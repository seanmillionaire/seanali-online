name: Force links pixel now

on:
  push:
    paths:
      - '.github/workflows/force-links-pixel-now.yml'

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Meta Pixel on links page
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('links.html')
          s = p.read_text(encoding='utf-8')
          pixel_id = '1586097145783190'
          if pixel_id not in s:
              block = '''<!-- Meta Pixel Code -->
<script>
!function(f,b,e,v,n,t,s)
{if(f.fbq)return;n=f.fbq=function(){n.callMethod?
n.callMethod.apply(n,arguments):n.queue.push(arguments)};
if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
n.queue=[];t=b.createElement(e);t.async=!0;
t.src=v;s=b.getElementsByTagName(e)[0];
s.parentNode.insertBefore(t,s)}(window, document,'script',
'https://connect.facebook.net/en_US/fbevents.js');
fbq('init', '1586097145783190');
fbq('track', 'PageView');
</script>
<noscript><img height="1" width="1" style="display:none"
src="https://www.facebook.com/tr?id=1586097145783190&ev=PageView&noscript=1"
/></noscript>
<!-- End Meta Pixel Code -->
'''
              s = s.replace('</head>', block + '</head>', 1)
              p.write_text(s, encoding='utf-8')
          PY
          rm -f .github/workflows/force-links-pixel-now.yml
          rm -f .github/workflows/add-fb-pixel-links-once.yml
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add links.html .github/workflows/force-links-pixel-now.yml .github/workflows/add-fb-pixel-links-once.yml
          git commit -m "Install Meta Pixel on links page"
          git push